# Dependencias migradas a pyproject.toml para uso con uv.
# Este archivo queda solo como referencia para entornos que todavia usen pip.
django>=5.0,<6.0
django-environ>=0.11,<1.0
pillow>=10.0,<12.0
psycopg[binary]>=3.1,<4.0
